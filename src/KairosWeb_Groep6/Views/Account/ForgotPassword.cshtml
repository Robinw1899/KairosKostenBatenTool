@model ForgotPasswordViewModel
@{
    ViewData["Title"] = "Wachtwoord vergeten?";
}

<div class="row">
    <div class="box-login">
        <h2>@ViewData["Title"]</h2>
        <form asp-controller="Account" asp-action="ForgotPassword" method="post" class="form-horizontal">
            <h6 class="infoTekstVerplichteVelden">Velden aangeduid met een * zijn verplicht in te vullen</h6>
            <h4>Vul je emailadres in.</h4>
            <hr/>
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group required">
                <label asp-for="Email" class="col-md-2 control-label"></label>
                <div class="col-md-10">
                    <input asp-for="Email" class="form-control input-maxwidth" placeholder="voorbeeld@example.com"/>
                    <!--<span asp-validation-for="Email" class="text-danger"></span>-->
                </div>
            </div>
            <div class="row col-md-offset-2">
                <div class="col-md-3">
                    <a class="btn btn-default">Annuleer</a>
                </div>
                <div class="col-md-5 text-right">
                    <button type="submit" class="btn btn-info">Reset wachtwoord</button>
                </div>
            </div>
        </form>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" type="text/css" href="/css/login.css" />
    <style>
        .body-content {
            background: none;
        }
    </style>
}

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    
    <script>
        var body = document.getElementsByTagName("body")[0];
        body.style.backgroundImage = "url(/images/aanmelden_bg.jpg)";
    </script>
}
