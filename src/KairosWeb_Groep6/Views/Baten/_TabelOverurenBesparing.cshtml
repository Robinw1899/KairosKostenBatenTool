@model IEnumerable<KairosWeb_Groep6.Models.BatenViewModels.OverurenBesparingViewModel>

@*@if (!Model.Any())
    {
        <tr>
            <th>Jaarbedrag omzetverlies</th>
        </tr>
        <tr>
            <td>-</td>
        </tr>
    }*@
<table class="table table-striped table-condensed table-bordered">
    <tr>
        <th>Jaarbedrag</th>

    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Bedrag</td>
        </tr>
    }
</table>